diff --git a/grub2/util/getroot.c b/grub2/util/getroot.c
index 8239363..4d0f57b 100644
--- a/grub2/util/getroot.c
+++ b/grub2/util/getroot.c
@@ -511,6 +511,7 @@ char *
 grub_util_get_grub_dev (const char *os_dev)
 {
   char *grub_dev;
+  char *dest=NULL;
 
   switch (grub_util_get_dev_abstraction (os_dev))
     {
@@ -600,6 +601,14 @@ grub_util_get_grub_dev (const char *os_dev)
       grub_dev = grub_util_biosdisk_get_grub_dev (os_dev);
     }
 
+  if (dest=strstr(grub_dev,"/evms/"))
+    {
+        char *tail=(char *)malloc(strlen(strstr(grub_dev,"/evms/")+5)+1);
+        strncpy(tail,strstr(grub_dev,"/evms/")+5,strlen(strstr(grub_dev,"/evms/")+5)+1);
+        strcpy(dest,tail);
+        free(tail);
+    }
+
   return grub_dev;
 }
 
